[project]
name = "format-it"
version = "0.2.1"
description = "将 Word 文档进行格式化"
requires-python = ">=3.12"
dependencies = [
    "python-docx>=1.1.0",
    "pillow>=10.0.0",
    "tomli-w>=1.0.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "python-multipart>=0.0.18",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
